import { test } from "@playwright/test";

test.describe.skip("Cadastro real de OS legado", () => {
  test("substituido pelo Nivel 2 em level2-crud.spec.ts", async () => {
    // O teste antigo criava OS sem rotina de limpeza.
    // Os cadastros reais agora ficam em level2-crud.spec.ts.
  });
});
